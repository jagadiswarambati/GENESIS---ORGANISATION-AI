import { MissionInitiation } from "@/components/mission/mission-initiation";

/** Genesis begins with the mission, never a dashboard. */
export default function HomePage(): React.JSX.Element {
  return <MissionInitiation />;
}
